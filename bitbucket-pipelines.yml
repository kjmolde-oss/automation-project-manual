pipelines:
  branches:
    dev:
      - step:
          name: 🏗️ Build and Push Docker Image
          runs-on:
            - self.hosted
            - linux.shell
          script:
            # Mandate the use of the host's UNIX socket for Docker communication
            - export DOCKER_HOST=unix:///var/run/docker.sock
            
            - echo "Logging in to Docker Hub..."
            - echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

            - IMAGE_NAME="${DOCKERHUB_USERNAME}/automation-project"
            - VERSION="prod-0.1.${BITBUCKET_BUILD_NUMBER}"
            - VERSIONED_IMAGE="${IMAGE_NAME}:${VERSION}"

            - echo "Building Docker image..."
            - docker build -t "${IMAGE_NAME}:latest" .
            - docker tag "${IMAGE_NAME}:latest" "${VERSIONED_IMAGE}"

            - echo "Pushing images..."
            - docker push "${IMAGE_NAME}:latest"
            - docker push "${VERSIONED_IMAGE}"
            - echo "Image pushed successfully!"

      - step:
          name: 🚀 Deploy to Kubernetes Cluster
          runs-on:
            - self.hosted
            - linux.shell
          deployment: Production
          script:
            # FIX 1: Change directory to the repository root FIRST
            - cd "${BITBUCKET_CLONE_DIR}" 
            
            # FIX 2: Define the K8S_MANIFEST_PATH using a RELATIVE path from the root
            - export K8S_MANIFEST_PATH="k8s/deployment.yaml"
            
            # 3. Define the full image tag
            - DEPLOY_IMAGE="${DOCKERHUB_USERNAME}/automation-project:${VERSIONED_IMAGE}"
            
            # 4. Patch the YAML file (The 'sed' command now works!)
            - echo "Patching manifest at ${K8S_MANIFEST_PATH} with new image tag"
            - sed -i "s|<IMAGE_PLACEHOLDER>|${DEPLOY_IMAGE}|g" "${K8S_MANIFEST_PATH}"
            
            # 5. Apply the updated manifest to the Kubernetes cluster
            - echo "Applying Kubernetes deployment via kubectl..."
            - kubectl apply -f "${K8S_MANIFEST_PATH}"
            
            # 6. Wait for the deployment to finish rolling out
            - kubectl rollout status deployment/automation-project-deployment
            
            - echo "✅ Deployment to Kubernetes successful!"