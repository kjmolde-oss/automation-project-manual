pipelines:
  branches:
    dev:
      - step:
          name: 🏗️ Build and Push Docker Image
          runs-on:
            - self.hosted
            - linux.shell
          script:
            # CI Step: Pushes fresh build with --no-cache guarantee
            - export DOCKER_HOST=unix:///var/run/docker.sock
            - echo "Logging in to Docker Hub..."
            - echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            - IMAGE_NAME="${DOCKERHUB_USERNAME}/automation-project"
            - VERSION="prod-0.1.${BITBUCKET_BUILD_NUMBER}"
            - VERSIONED_IMAGE="${IMAGE_NAME}:${VERSION}"
            
            - echo "Building Docker image (Forcing fresh build)..."
            # --no-cache ensures the new code is built and pushed
            - docker build --no-cache -t "${IMAGE_NAME}:latest" . 
            
            - docker tag "${IMAGE_NAME}:latest" "${VERSIONED_IMAGE}"
            - echo "Pushing images..."
            - docker push "${IMAGE_NAME}:latest"
            - docker push "${VERSIONED_IMAGE}"
            - echo "Image pushed successfully!"

      - step:
          name: 🚀 Deploy via Ansible to LXD Target
          runs-on:
            - self.hosted
            - linux.shell
          deployment: Production
          script:
            # 1. Install necessary Python and Ansible tools
            # Note: openssh-client is required for Ansible connectivity
            - apt-get update && apt-get install -y python3-pip openssh-client
            
            # FIX: Use python3 -m pip to execute the installation immediately
            - python3 -m pip install ansible 
            
            # 2. Configure SSH Key for Ansible (The rest of the script remains the same)
            - mkdir -p ~/.ssh
            - echo "$SSH_PRIVATE_KEY" | sed 's/\r$//' > ~/.ssh/id_rsa
            - chmod 600 ~/.ssh/id_rsa
            
            # 3. Add Host Fingerprint
            - ssh-keyscan -H 10.81.37.50 >> ~/.ssh/known_hosts
            
            # 4. Execute Ansible Playbook
            - echo "Executing Ansible playbook for remote deployment..."
            - ansible-playbook -i inventory.ini deploy.yml \
              --private-key ~/.ssh/id_rsa \
              --extra-vars "target_host=ansible_target_node"

            - rm ~/.ssh/id_rsa
            - echo "✅ Deployment via Ansible completed successfully!"