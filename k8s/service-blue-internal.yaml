apiVersion: v1
kind: Service
metadata:
  name: automation-project-blue-internal
spec:
  type: ClusterIP
  selector:
    app: automation-project
    version: blue
  ports:
  - port: 80
    targetPort: 3000