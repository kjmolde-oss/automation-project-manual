apiVersion: v1
kind: Service
metadata:
  name: automation-project-blue-internal
spec:
  type: NodePort
  selector:
    app: automation-project
    version: blue
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30100