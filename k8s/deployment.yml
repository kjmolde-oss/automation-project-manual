# File: k8s/deployment.yml (Corrected Structure)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: automation-project-deployment
  labels:
    app: automation-project
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: automation-project
  template:
    metadata:
      labels:
        app: automation-project
    spec:
      containers: # (Likely Line 20)
      - name: automation-project
        image: <IMAGE_PLACEHOLDER> 
        ports:
        - containerPort: 3000 
--- # <--- (If this line is 26, the error is immediately before it)
apiVersion: v1
kind: Service
metadata:
  name: automation-project-service
spec:
  selector:
    app: automation-project
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer